plugins {
    id "com.android.application"

    id 'com.google.gms.google-services'
    id 'com.google.firebase.crashlytics'

    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "co.geeksempire.eresse"

    compileSdk 36
    ndkVersion "25.2.9519653"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "co.geeksempire.eresse"

        minSdkVersion 26
        targetSdkVersion 36

        versionCode 1
        versionName "${majorVersion}." + getDateMonth() + "." + getDateDay() + "." + getDateYear() + ".${versionCode}"
    }

    signingConfigs {
        release {
            storeFile file("D:\\Xine\\Android\\AppsScreenshotsKeys\\ArwenAI\\arwen.jks")
            storePassword '*arwen#10296$'
            keyAlias 'arwenai'
            keyPassword '*arwen#10296$'
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
        debug {
            debuggable true
        }
    }
}

flutter {
    source = "../.."
}

static def getDateYear() {
    def date = new Date()
    def formattedDate = date.format('yyyy')//yyyyMMdd
    return formattedDate
}

static def getDateMonth() {
    def date = new Date()
    def formattedDate = date.format('MM')//yyyyMMdd
    return formattedDate
}

static def getDateDay() {
    def date = new Date()
    def formattedDate = date.format('dd')//yyyyMMdd
    return formattedDate
}

dependencies {
    implementation 'com.google.android.material:material:1.12.0'
}